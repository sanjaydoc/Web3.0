{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "ACP Node",
  "version": "0.1.0",
  "identifier": "com.web3.acpnode",
  "build": {
    "frontendDist": "../../apps/dashboard/dist",
    "beforeBuildCommand": "pnpm --dir ../.. --filter @acp/dashboard build"
  },
  "app": {
    "windows": [
      {
        "title": "ACP Node · Web3.0",
        "width": 1280,
        "height": 840,
        "minWidth": 900,
        "minHeight": 600
      }
    ],
    "security": { "csp": null }
  },
  "bundle": {
    "active": true,
    "targets": ["msi", "nsis", "dmg", "appimage", "deb"],
    "icon": ["icons/icon.png"],
    "shortDescription": "Run a Web3.0 ACP node with a one-click app.",
    "longDescription": "A native desktop shell for the ACP node + dashboard — double-click to run a node and manage it in a window, no terminal."
  }
}
