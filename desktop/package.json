{
  "name": "@acp/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "ACP Node desktop app (Tauri) — MSI / dmg / AppImage installers for a one-click node.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
