{
  "name": "web3-mobile",
  "version": "0.1.0",
  "description": "Web3.0 mobile — a Capacitor Android app that runs a REAL peer node (nodejs-mobile) and shows the dashboard.",
  "private": true,
  "author": "DR SANJAY ANBU",
  "homepage": "https://github.com/sanjaydoc/Web3.0",
  "scripts": {
    "prepare:app": "node scripts/prepare.mjs",
    "sync": "npm run prepare:app && cap sync android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "capacitor-nodejs": "https://github.com/hampoelz/Capacitor-NodeJS/releases/download/v1.0.0-beta.10/capacitor-nodejs.tgz"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.24.0"
  }
}
